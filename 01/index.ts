/*const fs = require("fs")

const leitura = (): unknown =>{
    return JSON.parse(fs.readFileSync('./bd.json'))
}


const escrita = (dados: any): void =>{
    fs.writeFileSync('./bd.json', JSON.stringify(dados))
}


type Pessoa = {
    nome: String,
    idade: number
}

const pessoa: Pessoa={
    nome: 'AnaXUXUMEBEJSA',
    idade: 19
}

const dados = leitura() as Pessoa[]
dados.push(pessoa)
//console.log(dados);

escrita(dados)
console.log(leitura());*/
